FROM python:3.12-slim

WORKDIR /srv/app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY worker ./worker

RUN useradd --create-home --uid 1000 appuser
USER appuser

EXPOSE 8002

CMD ["uvicorn", "worker.main:app", "--host", "0.0.0.0", "--port", "8002"]
